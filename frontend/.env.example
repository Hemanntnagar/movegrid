# Optional. Leave unset to run the full in-browser demo (zero Vercel env vars).
# Set this only when pointing at a real backend:
# NEXT_PUBLIC_API_URL=https://your-api.example.com/api/v1
# Used by /api/fitness-plan/generate in demo mode (when no backend URL is set)
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-3.8-flash
